import React from "react";

const LandingPage = ({name}) => {
    return (
        <div>
        <h1>Stan App</h1>
        <h2>Welcome {name}</h2>
        </div>
    );
    };

export default LandingPage;